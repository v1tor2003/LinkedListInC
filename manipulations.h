#ifndef MANIPULATIONS_H_INCLUDED
  #define MANIPULATIONS_H_INCLUDED
#endif

#include "interface.h"

int get_element();
int get_option();
int count_elements(Node* list);
int find_bigger(Node* list_A);
int elements_equal_N(Node *list, int N);
int search_for_N(Node* list, int N);

